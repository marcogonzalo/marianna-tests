export enum ScoringMethod {
    BOOLEAN = 'boolean',
    SCORED = 'scored',
    CUSTOM = 'custom',
}

export enum ResponseStatus {
    PENDING = 'pending',
    COMPLETED = 'completed',
    ABANDONED = 'abandoned',
    DISCARDED = 'discarded',
}
